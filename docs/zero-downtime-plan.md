# Бесшовная перезагрузка версии (zero-downtime reload) — дизайн и реализация

> Статус: РАЗРАБОТАНО И ПРОВЕРЕНО ЛОКАЛЬНО 2026-07-12 (живой reload под нагрузкой:
> 200 запросов во время свопа, 0 ошибок, туда и обратно). Прод-механика (blue-green
> systemd) написана, ждёт хоста. Запрос пользователя: «запретить на старом сервере
> новые записи, перенаправить на новый, дождаться обработки, убить старый».

## Ключевое решение: НЕ разделять чтения/записи — переключать ВЕСЬ трафик атомарно

Интуиция «запретить записи на старом» упрощается благодаря нашей архитектуре:
- **сервер stateless**: каждый HTTP-запрос = ровно одна PG-транзакция (runCxmTx over
  connectPerTxn), между запросами в процессе нет состояния; сессии — JWT (без стора);
- значит «дождаться обработки записей» = дождаться завершения in-flight HTTP-запросов
  (секунды), никаких очередей внутри процесса;
- обе версии могут КОРРЕКТНО писать в PG одновременно (транзакции сериализует Postgres),
  если схема совместима (см. «Миграции»). Разделение read/write добавило бы нюансов
  (классификация запросов, sticky-маршрутизация) без выгоды.

Итоговая схема (blue-green):
```
1. новый бинарь стартует ВТОРЫМ процессом (свой сокет/порт); его boot гонит миграции
2. health-гейт нового (+опц. смоук)
3. АТОМАРНЫЙ своп точки входа → новые запросы идут новому
4. старому SIGTERM → warp перестаёт принимать, дорабатывает in-flight (graceful, 30с) → exit
5. (откат: своп назад, пока старый жив — окно между 3 и 4 можно растянуть на проверку)
```

## Нюансы и их закрытие (по одному на строку кода)

| Нюанс | Решение | Где |
|---|---|---|
| Обрыв in-flight по SIGTERM | Warp graceful: setInstallShutdownHandler(TERM/INT → closeSocket) + setGracefulShutdownTimeout 30с — процесс сам выходит после дренажа | agdelte `hs/Agdelte/Http.hs` (serveHost/serveUnix/serveWithWs) |
| Атомарность переключения | прод: nginx `proxy_pass http://unix:.../active.sock` — СИМЛИНК, connect() резолвит путь на каждое соединение, `ln -sfn` атомарен; локально: прокси читает цель из файла на КАЖДЫЙ запрос, swap = mv файла | `deploy/nginx-psych.conf.tmpl`, `reload-site.sh`; `site-psych/dev/serve.mjs` (CXM_API_FILE), `host-local.sh reload` |
| Двойные ПИСЬМА в overlap (два воркера) | claim АТОМАРЕН: `claimOutboxV` = lockRoot (FOR UPDATE) + попытка учтена ПРИ claim'е (lastAttempt=now ⇒ у второго строка не due, backoff-щит); успех → markSent, провал — ничего (попытка уже посчитана) | ядро `Cxm.CommandsV.claimOutboxV`, сервер `deliverOne` |
| Двойные напоминания/bus в overlap | ЛИДЕР-ГЕЙТ тика: `tryLockKey nsWorker` (новый verb `rTryLockKey` = pg_try_advisory_xact_lock; парс через `SELECT 1 AS id WHERE …` — без нового декодера); remind+bus+выборка в ОДНОЙ транзакции под локом, не-лидер скипает тик | store-verb `Cxm.Store.Verbs` + `Pg`/`VerbsTest`; сервер `workerTick` |
| Миграции: старый бинарь на новой схеме | дисциплина УЖЕ Tier-1: хвостовые nullable-колонки, tolerant-декод; старый INSERT без новой колонки → NULL → ок. Опасное (DROP/RENAME/NOT NULL) — только expand→deploy→contract (contract отдельным релизом, когда старой версии нет). Ledger (schema_migrations) идемпотентен — повторный boot нового безвреден | конвенция + `Cxm.Store.Registry` (watch-refl) |
| Старые JS-бандлы во вкладках клиентов | contract-version в /health + сверка сайтом (красный баннер дрейфа) УЖЕ есть; правило аддитивности API соблюдаем | CxmUI.Contract.expectedContract |
| Вебхуки ЮKassa в окно свопа | редоставка идемпотентна (claim по extId, «granted:false» 200) — провайдер доретраит в любой из серверов | PsychCxm.Server.webhookTx |
| PG-пул | пула нет (connectPerTxn) — дренировать нечего; pgbouncer/v2 не меняет схему (пул вне процесса) | — |
| media-host/статика | отдельные процессы, версия сервера их не трогает; их own reload — тем же приёмом (нечасто нужен) | — |
| Зависший дренаж | graceful timeout 30с в warp + systemd TimeoutStopSec (юнит) / kill -9 в host-local через 35с | юниты, host-local.sh |

## Механика

**Локально** (`psych-platform/deploy/local/host-local.sh reload`): пара портов
API_PORT/API_PORT_ALT (8138/8139); активный — в файле `$STATE/api-target`, статик-прокси
читает его на каждый запрос. reload: старт нового на неактивном порту → health →
mv api-target → SIGTERM старому → ожидание выхода (35с, потом kill -9).
ПРОВЕРЕНО: 200 health-запросов через прокси во время свопа — 0 не-200; обратный
reload симметричен.

**Прод** (`psych-platform/deploy/reload-site.sh <slug>`): юниты
`cxm-server-pg-{blue,green}@<slug>` (сокеты `/run/sites/<slug>/{blue,green}.sock`),
nginx проксирует на симлинк `active.sock`. reload: старт неактивного цвета →
health через `curl --unix-socket` → `ln -sfn` → `systemctl stop` старого цвета
(SIGTERM → graceful). Откат = повторный запуск скрипта. На реальном хосте НЕ гонялся —
первый прогон по шагам.

## Чего сознательно НЕТ

- **Read-write разделения** — см. выше: атомарный своп всего трафика проще и достаточен.
- **ИНВАРИАНТ КОНФИГА (уточнение 2026-07-12):** браузер ДЕРЖИТ постоянные соединения
  (keep-alive/HTTP-2) — но они терминируются на фронтовом прокси; бесшовность стоит на том,
  что прокси коннектится к бекенду PER-REQUEST. Поэтому: nginx upstream `keepalive` НЕ
  включать, не переосмыслив своп (прогретые соединения продолжат указывать на старый сокет;
  warp graceful закрывает idle — деградация мягкая, но это уже нюанс). Локальный фронт
  serve.mjs своего graceful-reload не имеет — на лету не перезагружать (прод-фронт nginx
  reload штатно graceful).
- **Session-draining sticky-логики** — сессий нет (JWT); браузерный keep-alive живёт с
  ФРОНТОВЫМ прокси (не перезагружается), к бекенду прокси коннектится per-request —
  «подключённые клиенты» свопа не видят (2026-07-12: вопрос пользователя, разбор по видам
  подключённости — HTTP/JWT/стримы/poll — все переживают; единственный будущий нюанс —
  пуш-стримы: при выходе старого сервера порвутся; решение 2026-07-12 — пуш делать SSE
  (авто-reconnect + Last-Event-ID встроены в стандарт, браузер переподключится к новому
  серверу сам), HTTP/3 — терминацией на nginx, бекенд/своп не затронуты; НЕ WS (там reconnect
  пришлось бы писать руками).
- **Оркестратора** — один скрипт на инстанс; флот перезагружается пер-слагово
  (канареечно: обновил один сайт, посмотрел, остальные).

## Производительность на нагрузке (вопрос пользователя 2026-07-12: «per-request — медленно?»)

Лестница стоимостей одного запроса (порядки величин):
- прокси→бекенд unix connect: ~единицы МИКРОсекунд (без handshake/TLS) — не узкое место;
- локальный loopback TCP connect: десятки µs (потолок TIME_WAIT — на десятках тысяч RPS);
- **свежий PG-коннект (connectPerTxn): единицы–десятки МИЛЛИсекунд (fork на стороне PG) —
  ВОТ первое настоящее узкое место на нагрузке**;
- сама PG-транзакция запроса: единицы ms.

Порядок починки (оба шага совместимы со свопом):
1. **pgbouncer** (pg-store-plan: «pgbouncer/v2 — только раннер») — transaction-pooling ВНЕ
   процесса: CXM_PG → pgbouncer, тёплый пул к PG; правок кода ~0; свопу безразличен
   (пул живёт вне обеих версий).
2. (сильно позже, если упрёмся в прокси) nginx upstream keepalive — допустимо: warp при
   graceful закрывает idle-соединения; перед включением прогнать смоук
   «200 запросов во время свопа» с пулом.

## АУДИТ РЕШЕНИЯ (2026-07-13, адверсарии: живые тесты + разбор кода)

Метод: два сервера на ОДНОЙ БД (медленный sendmail-счётчик, тики 2с) + дренаж-тест
записей + разбор кода/юнитов/скриптов.

**Живые тесты:**
- ✓ Т1 (одиночность доставки при overlap): 3 брони → 6 писем (3 подтверждения +
  3 напоминания — НЕ дубли) → каждое доставлено РОВНО один раз (attempts=1, sent)
  при двух гоняющихся воркерах с 2-секундной отправкой. claim + лидер-лок работают.
- ✓ Т2 (дренаж ЗАПИСЕЙ): 40 параллельных POST /v1/events + SIGTERM в полёте →
  принято 200: 40, закоммичено строк: 40, потерь ноль; процесс вышел сам.
- ✗→✓ №1 (НАЙДЕНО ЖИВЬЁМ): параллельный boot двух серверов на СВЕЖЕЙ БД падал —
  гонка bootMigrations (CREATE IF NOT EXISTS → duplicate key в pg_type). Фикс:
  session-advisory-лок (pg_advisory_lock(42,0)) на boot-соединении. Перепроверено:
  двойной boot на свежей БД — оба 200.

**Разбор кода (найдено → исправлено):**
- ✗→✓ №3 off-by-one в claimOutboxV: гейт `maxAtt ≤ suc attempts` съедал последнюю
  попытку (7 отправок вместо 8) → гейт по УЖЕ сделанным попыткам.
- ✗→✓ №4 reload-site.sh: `systemctl start` нового цвета был бы no-op со СТАРЫМ
  бинарём, если цвет уже бежал (сбойный прошлый reload) → restart.
- ✗→✓ №5 /run — tmpfs: симлинк active.sock исчезает на ребуте (nginx проксировал бы
  в никуда) → ExecStartPre цветных юнитов воссоздаёт его УСЛОВНО (только если
  отсутствует — обычный reload не перехватывает трафик до health-гейта).
- ✗→✓ №6 откат был заявлен шире, чем есть: честное окно отката = ДО stop старого
  цвета; после — bin уже перезаписан, откат = redeploy (держать bin.prev). Комментарий
  скрипта исправлен.
- ✗→✓ №7 параллельный запуск reload не гардился → mkdir-лок в reload-site.sh и
  host-local.sh (trap снимает; /run-лок умирает с ребутом).
- ✓ file-target прокси: mv атомарен, чтение видит старую или новую цель, частичной не
  бывает; отсутствие файла — фолбэк на env.
- ✓ ложная тревога Т1-первого прогона: «6 доставок на 3 брони» оказались напоминаниями
  (lead 24h) — урок аудиту: считать ожидание от СОДЕРЖИМОГО outbox, не от числа действий.

**Пост-фикс верификация:** смоук 88/88 ×2, стражи OK, Test/All OK, параллельный boot OK.
Остаётся (заявлено и раньше): прод-скрипты на реальном хосте не гонялись — первый
катовер по шагам; nginx-symlink-swap проверен только логикой connect-per-request.

## АУДИТ-2 (2026-07-13, повторный заход: аудит фиксов + непокрытые слои)

**Найдено → исправлено:**
- ✗→✓ №2-1 ExecStartPre юнитов: `[ -e … ]` на СИМЛИНКЕ проверяет ЦЕЛЬ (dangling-линк = false
  → цвет перехватил бы маршрут) → `[ -L … ]` (существование самого линка).
- ✗→✓ №2-2 reload-site: enable нового цвета / disable старого — иначе после ребута стартовал
  бы НЕ активный цвет (безвредно при одинаковом bin, но грязно).
- ✗→✓ №2-3 ПОРЯДОК деплоя (реальная дыра процесса): rsync доставлял www+bin ВМЕСТЕ до
  reload → окно «новый JS ↔ старый API» (аддитивность гарантируется только в сторону
  «старый клиент — новый сервер»). README: сначала bin → reload-site, ПОТОМ www.
- ✗→✓ №2-4 покрытие: claimOutboxV не имел refl-тестов → добавлены три инварианта
  (claim→true+attempts; немедленный повтор→false по backoff; исчерпание→Failed без отправки).
  Все — дефиниционально (VerbsTest).

**Проверено, признано корректным (с фиксацией семантики):**
- ✓ crash-mid-send (kill -9 между claim и send): письмо остаётся Pending с attempts+1 →
  ретрай после backoff — доставка AT-LEAST-ONCE сохраняется.
- ✓ обратная сторона at-least-once: send успел, markSent НЕ успел (падение/PG-hiccup) →
  возможен ПОВТОР письма после backoff. Это врождённое свойство email-доставки без
  двухфазного коммита; принято (idempotency-key в теме письма — по потребности).
- ✓ DDL нового бинаря под трафиком старого: ADD COLUMN nullable в PG мгновенен (краткий
  ACCESS EXCLUSIVE → очередь на мс). ПРАВИЛО: table-rewrite DDL (ALTER TYPE, ADD COLUMN
  DEFAULT на старых PG) в ledger не класть / только офф-пик.
- ✓ deadlock тика с HTTP-запросами: цикла нет (nsWorker берётся ПЕРВЫМ и только воркером).
- ✓ stale-`now` в deliverAll (последовательные отправки при claim'ах со временем начала
  тика) — консервативно в безопасную сторону.
- ✓ rollback-на-старый-бинарь поверх новой схемы — это ровно инвариант Tier-1 (N на N+1).

**Пост-фикс:** VerbsTest (с новыми refl) OK; смоук/стражи прогонялись после аудита-1 —
кодовые правки аудита-2 транзитом через тайпчек. Остаток без изменений: прод-скрипты
живьём — при первом катовере.

## АУДИТ-3 (2026-07-13, третий заход: дрейф состояния и верификация тождества)

**Найдено живьём → исправлено → атака повторена:**
- ✗→✓ №3-1 (КРИТИЧЕСКАЯ, воспроизведена): остаточный процесс на неактивном порту
  (после сбойного прошлого reload) — reload «успешно» свопал трафик НА НЕГО: свежий
  процесс умирал на bind, health-гейт проходил за счёт чужака НЕИЗВЕСТНОЙ версии,
  pid-файл указывал на труп. Фикс: ПОРТ-ГЕЙТ в start_server_on (порт отвечает до
  старта → громкий отказ, активный сервер не тронут). Атака повторена после фикса —
  отбита («порт занят посторонним процессом», трафик остался на месте).
- ✗→✓ №3-2 reload не верифицировал ВЕРСИЮ — health-гейт не отличал старый бинарь от
  нового (тот же класс, что №3-1: «переключились, но на что?»). Фикс: build-стамп —
  сервер отдаёт CXM_BUILD в /health (аддитивно; local = mtime бинаря автоматически,
  prod — env per-deploy, site.env.example); reload печатает переход build'ов и
  предупреждает при совпадении. Проверено: штатный reload того же бинаря честно
  ругается «build не изменился».

**Проверено, чисто:**
- ✓ все интерпретаторы Req покрывают rTryLockKey (несовпадение паттернов не собралось бы;
  gen прошёл).
- ✓ слитая транзакция тика (remind+bus+выборка): abort любой части откатывает тик целиком —
  атомарность даже строже прежней; письма ждут следующего тика.
- ✓ serve.mjs/media-host не имеют graceful — и не участвуют в reload (их рестарт — редкое
  событие обслуживания, не деплой версии).
- ✓ healthDec клиента толерантен к новому полю build (field'-декодер читает только своё).

**Пост-фикс:** смоук 88/88, стражи OK, dogfood-reload хостинга с build-переходом.
Динамика находок: аудит-1 — 5, аудит-2 — 4, аудит-3 — 2 (обе одного корня: «гейт
проверял доступность, а не ТОЖДЕСТВО») — кривая сходится; следующий содержательный
слой проверки — только реальный хост.
